# 公共天菜库目录

本目录存放**公开菜单** `public_index.json`。

- 视频文件由维护者通过插件「发布到公共库」上传到 GitHub Release（标签默认 `tiancai-videos`）
- 普通用户只下载本菜单与视频，不能上传
- 维护者需在插件配置中填写 `github_token` 与 `admin_passphrase`（均不写进开源代码）

详见：`docs/PUBLIC_LIBRARY.md`
