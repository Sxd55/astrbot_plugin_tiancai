# 天菜视频库 · 功能探讨稿

仓库：https://github.com/sxd55/astrbot_plugin_tiancai  
定位：**多群共用一套全局天菜库**，同时在 AstrBot WebUI 提供插件管理页（Plugin Pages）。

本文目标：先对齐「要什么 / 不要什么 / 先做哪期」，再动手实现。

---

## 0. 现状（已有）

| 能力 | 现状 |
| --- | --- |
| 入库 | 回复视频后发「收进天菜」 |
| 出库 | 「看看天菜」随机发送 |
| 查询 | 「天菜数量」 |
| 清空 | 「清空天菜」（管理员） |
| 存储 | 全局库 `data/plugin_data/astrbot_plugin_tiancai/videos/` |
| 配置 | `_conf_schema.json`：目录名 / 上限 / 是否允许重复 |
| WebUI 页面 | **无** |

---

## 1. 产品原则（建议先定）

1. **全局库为主**：所有群共享同一批天菜；权限靠「谁能收 / 谁能看 / 谁能删」控制，而不是先做复杂分库。
2. **聊天指令负责高频操作，WebUI 负责管理与可视化**：群里随手收、随手抽；后台负责浏览、删除、导入导出、统计。
3. **安全默认**：入库默认需管理员或白名单；随机发送可对全员开放（可配置）。
4. **可恢复**：删除进回收站，清空需二次确认；重要操作写审计日志。
5. **先做闭环，再做花活**：UI 能看能删能导 → 指令补齐 → 自动化 → 精细权限。

---

## 2. 功能地图（按模块）

### A. 插件 UI（AstrBot Plugin Pages）——你点名要的

AstrBot 支持在插件目录放 `pages/<page_name>/index.html`，通过 `window.AstrBotPluginPage` 调后端 API。

建议做 **1 个主页面**：`pages/dashboard/`（天菜管理台）

#### A1. 总览面板
- 视频总数、占用磁盘、今日入库数、今日发送数
- 最近入库 / 最近发送流水
- 当前配置摘要（上限、权限模式、自动监听开关）

#### A2. 视频库浏览
- 卡片/列表：缩略图（若可提取）、文件名、大小、入库时间、来源群、收藏人
- 搜索：按编号、收藏人、来源群、备注
- 排序：时间 / 大小 / 被抽中次数
- 多选：批量删除、批量加标签、批量导出

#### A3. 单条管理
- 预览 / 下载原文件
- 编辑备注、标签（如 `#名场面` `#整活`）
- 软删除 / 恢复 / 永久删除
- 「设为置顶天菜」（看看天菜时可提高权重或固定池）

#### A4. 导入导出
- WebUI 上传视频入库（不经过 QQ）
- 导出：打包 zip / 导出索引 JSON
- 从备份 JSON 恢复元数据

#### A5. 设置页（可与 `_conf_schema` 并存，或 UI 直接改同一配置）
- 库上限、去重、存储目录
- 权限策略
- 自动化开关与频率
- 指令别名（可选）

#### A6. 日志与审计
- 谁在何时收进 / 删除 / 清空 / 手动上传
- 发送失败原因（协议端路径、callback 未配置等）

> 技术备注：缩略图需要 ffmpeg 或协议端封面字段；若环境没有 ffmpeg，UI 第一期可用「文件卡片 + 元信息」，缩略图放二期。

---

### B. 聊天指令扩展

| 指令草案 | 作用 | 建议权限 |
| --- | --- | --- |
| `收进天菜` | 已有：回复视频入库 | 管理员 / 白名单 |
| `看看天菜` | 已有：随机一条 | 全员（可关） |
| `天菜数量` | 已有 | 全员 |
| `清空天菜` | 已有 | 仅管理员 |
| `删除天菜 <编号>` | 删指定一条 | 管理员 / 原收藏人 |
| `天菜详情 <编号>` | 查看元信息 | 全员或管理 |
| `今日天菜` | 当天未发过的随机一条 | 全员 |
| `天菜标签 <标签>` | 按标签随机 | 全员 |
| `天菜搜索 <关键词>` | 搜备注/标签，返回编号列表 | 全员 |
| `置顶天菜 <编号>` | 加入高权重池 | 管理员 |
| `天菜帮助` | 指令说明 | 全员 |

可选趣味指令（低优先级）：
- `天菜连抽 3`（一次发多条，需限流）
- `天菜排行`（被抽次数 Top）
- `我的天菜`（看自己收藏了几条）

---

### C. 自动化 / 运营

| 功能 | 说明 | 风险 |
| --- | --- | --- |
| C1. 自动监听入库 | 群里出现视频就自动收（或需带关键词） | 易灌库、版权/隐私风险 |
| C2. 定时推送 | 每天/每周固定时间「来点天菜」 | 需存储会话 UMO |
| C3. 冷却与防刷 | 同用户 N 秒内只能抽一次 | 强烈建议做 |
| C4. 去重增强 | 按文件 hash（不只 message_id） | 需读文件算 hash |
| C5. 容量治理 | 超限时拒绝 / LRU 淘汰最久未抽中 | 需明确策略 |
| C6. 失败重试提示 | 发送失败时告诉用户检查 callback_api_base | 体验向 |

**建议默认：**
- 自动监听：**默认关闭**，仅白名单群可开
- 定时推送：二期
- 冷却：一期就做（例如 10–30 秒）

---

### D. 权限与隔离（多群统一库前提下）

统一库 ≠ 谁都能改。建议权限模型：

#### D1. 角色
- **超级管理**：AstrBot 管理员（已有 `PermissionType.ADMIN`）
- **天菜管理**：插件内额外 QQ 号白名单
- **普通成员**：只能「看看天菜」类只读指令

#### D2. 可配置策略
- `collect_policy`：`admin_only` / `whitelist` / `everyone`
- `view_policy`：`everyone` / `group_whitelist`
- `delete_policy`：`admin_only` / `owner_or_admin`
- `enabled_groups`：空=全群可用；非空=仅这些群能用指令
- `blocked_users`：黑名单

#### D3. 「隔离」在统一库下的折中
你选的是多群统一库，因此**不做物理分库**，但可做：
- 来源群标签（统计/筛选用）
- 某群「仅能抽、不能收」
- 某群禁用插件指令

若未来真要分库，可升级为：`library_mode = global | per_group | hybrid`（三期）。

---

## 3. 推荐分期（MVP → 完整）

### 一期（建议马上做）：「能管、能控、有 UI」
1. Plugin Page：总览 + 视频列表 + 删除/下载 + 基础上传
2. 指令：`删除天菜`、`天菜详情`、`天菜帮助`
3. 权限：入库默认管理员；看看天菜全员；冷却时间
4. 索引增强：备注、标签、抽中次数、软删除标记
5. API：`stats` / `list` / `delete` / `update` / `upload`

### 二期：「更好用」
1. UI：搜索筛选、批量操作、标签编辑、回收站
2. 指令：`今日天菜`、`天菜标签`、`天菜搜索`、`置顶天菜`
3. hash 去重、导出 zip、审计日志页
4. 缩略图（若有 ffmpeg）

### 三期：「运营向」
1. 自动监听入库（可配置关键词/白名单群）
2. 定时推送
3. 排行榜、连抽、权重随机
4. 可选 per-group 库模式

---

## 4. UI 信息架构草案

```text
天菜管理台
├─ 总览（数字卡片 + 最近动态）
├─ 视频库（列表/卡片，核心）
├─ 回收站
├─ 设置（权限 / 冷却 / 自动化）
└─ 日志
```

一期可先只做：**总览 + 视频库 + 设置（精简）**。

---

## 5. 数据模型升级草案

当前 `index.json` 较瘦，建议扩展为：

```json
{
  "version": 2,
  "videos": [
    {
      "id": "ab12...",
      "filename": "ab12....mp4",
      "saved_at": 1690000000,
      "source_message_id": "...",
      "source_group_id": "...",
      "collector_id": "...",
      "collector_name": "...",
      "size": 1234567,
      "note": "名场面",
      "tags": ["整活", "名场面"],
      "play_count": 3,
      "pinned": false,
      "deleted_at": null,
      "sha256": "..."
    }
  ],
  "settings_override": {}
}
```

兼容：启动时若发现旧索引，自动迁移到 v2。

---

## 6. 需要你拍板的关键分歧

下面这些问题会直接影响一期范围：

### 6.1 谁可以「收进天菜」？
- A. 仅 AstrBot 管理员（最稳）
- B. 管理员 + 插件白名单 QQ
- C. 群里所有人

### 6.2 自动监听要不要进一期？
- A. 不做，继续手动回复入库（推荐）
- B. 做，但默认关，且仅白名单群

### 6.3 UI 第一期要不要「上传视频」？
- A. 要（方便补库，不依赖群消息）
- B. 先不做，只管理已有文件

### 6.4 删除策略
- A. 软删除（回收站，可恢复）——推荐
- B. 直接物理删除

### 6.5 「看看天菜」随机策略
- A. 纯随机
- B. 降低近期已发过的权重（少重复）
- C. 置顶池优先 + 普通池

---

## 7. 明确建议先不做（避免一期膨胀）

- 完整分群独立库
- AI 自动打标签 / 鉴黄
- Web 在线播放大视频转码
- 复杂工作流引擎
- 跨 bot 实例同步

---

## 8. 一期交付清单（若按本文建议）

**代码**
- `pages/dashboard/index.html` + `app.js` + `style.css`
- `main.py` 增加 Web API + 新指令 + 索引迁移
- `_conf_schema.json` 增加权限/冷却配置
- README 更新

**体验**
- WebUI 能看到库、删视频、下视频、（可选）上传
- 群里能删指定天菜、看详情、有帮助
- 防刷冷却生效

---

## 9. 讨论结论（已拍板 · 2026-09-10）

| 议题 | 结论 |
| --- | --- |
| 总体范围 | **大而全**（不含自动监听） |
| 使用场景 | 多群统一全局库 |
| 入库权限 | 管理员 + QQ 白名单 |
| 删除策略 | 软删除 + 回收站可恢复 |
| WebUI 上传 | 要 |
| 自动监听 | **不做**（可留三期开关位，但本轮不实现） |
| 随机策略 | 降权少重复（近期发过的降低权重） |
| 推进方式 | **分批推送，边做边测** |

### 本轮要做的模块

1. 完整管理台 UI（总览 / 列表搜索 / 备注标签 / 回收站 / 上传下载 / 设置 / 日志）
2. 全套聊天指令（删除、详情、帮助、今日天菜、按标签、搜索、置顶、连抽、排行、我的天菜）
3. 运营治理（冷却防刷、hash 去重、容量治理、降权随机）
4. 定时推送到指定群

### 分批实现顺序（建议）

| 批次 | 内容 | 验收点 |
| --- | --- | --- |
| **Batch 1** | 索引升级 v2 + 权限/冷却配置 + `删除天菜`/`天菜详情`/`天菜帮助` + 降权随机 | ✅ 已完成并推送 v1.1.0 |
| **Batch 2** | Plugin Page 管理台 + stats/list/delete/restore/purge/update/upload/download/logs API | ✅ 已完成 v1.2.0 |
| **Batch 3** | 顺序编号 #1… / 默认标签「天菜」 / 卡片内视频预览 | ✅ 代码完成 v1.3.0 |
| **Batch 4** | 公共天菜源（GitHub index + URL，只读同步/缓存/mixed）+ 运营文档 | ✅ 代码完成 v1.4.0（待你 push） |
| **Batch 3** | 标签备注编辑、搜索筛选、设置页、审计日志 | 管理台闭环 |
| **Batch 4** | 今日天菜 / 标签抽 / 搜索 / 置顶 / 连抽 / 排行 / 我的天菜 | 指令全家桶 |
| **Batch 5** | hash 去重、容量治理、定时推送 | 运营能力就绪 |

每批做完推送到 `sxd55/astrbot_plugin_tiancai`，你装上测一轮再开下一批。
